import AppProcessor from '../_core/app.processor';

/**
 * The StaffProcessor class
 */
export class ResourceProcessor extends AppProcessor {
	/**
	 * @param {Model} model required for response
	 */
	constructor(model) {
		super(model);
	}
}
